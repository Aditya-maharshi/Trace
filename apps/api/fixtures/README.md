# Fixtures

This directory contains cached JSON responses from external APIs (e.g. Etherscan, OpenSanctions).
These are **DEMO / TEST data fixtures**, NOT a live cache.
During a live demo or production use, the attribution engine will fetch real-time data from the blockchain and APIs. Do not confuse this directory with a live production cache.
